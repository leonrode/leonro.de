const Header = ({ text }) => (
  <h1 className="mt-8 text-center font-bold bg-gradient-to-r py-2 from-primary to-darkPrimary bg-clip-text text-transparent text-5xl">
    {text}
  </h1>
);

export default Header;
